import { defineConfig } from 'astro/config';

// Static personal portfolio for Sawat Wongsaroj.
// Change `site` to your deployed URL before publishing (used for canonical + sitemap).
export default defineConfig({
  site: 'https://sawatwongsaroj.com',
  compressHTML: true,
});
